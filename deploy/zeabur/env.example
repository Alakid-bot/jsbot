# Zeabur service environment variables for this Discord bot.
# Do NOT commit real secrets. Copy values into Zeabur Dashboard > Service > Environment Variables.

# Web configuration page password. zeabur-template.yaml injects the same fixed
# Zeabur-generated value into PASSWORD and JSBOT_WEB_PASSWORD, then shows it as
# the "Web configuration password" deployment instruction for copying. If both
# are omitted, the container generates a 16-character fallback, saves it to
# JSBOT_WEB_PASSWORD_FILE, and prints it in logs.
# PASSWORD=ZEABUR_GENERATED_PASSWORD
# JSBOT_WEB_PASSWORD=ZEABUR_GENERATED_PASSWORD
JSBOT_WEB_PASSWORD_FILE=/app/data/web-password.txt
PORT=8080
JSBOT_CONFIG_PATH=/app/data/config.json

# Optional initial config. If omitted, open the Zeabur domain and create it from
# the web page. Recommended: base64 encode JSON locally when you want to seed it.
# JSBOT_CONFIG_JSON_BASE64=BASE64_ENCODED_CONFIG_JSON

# Alternative to JSBOT_CONFIG_JSON_BASE64. Keep valid JSON in one value.
# JSBOT_CONFIG_JSON={"token":"YOUR_DISCORD_BOT_TOKEN","guilds":{}}

# PostgreSQL is required for runtime storage. If you deploy this bot with
# zeabur-template.yaml, PostgreSQL variables are injected automatically and you
# usually do not need to set JSBOT_PG_CONFIG_JSON_BASE64.
# JSBOT_PG_CONFIG_JSON_BASE64=BASE64_ENCODED_PG_CONFIG_JSON

# Alternative to JSBOT_PG_CONFIG_JSON_BASE64.
# JSBOT_PG_CONFIG_JSON={"host":"HOST","port":5432,"database":"DB","user":"USER","password":"PASSWORD","logging":false}

# Optional external read-only API for website/VPS statistics. This is
# intentionally separate from JSBOT_PG_CONFIG_JSON, which remains the bot
# writable runtime database.
# JSBOT_EXTERNAL_READONLY_API_URL=http://152.53.231.224:8787
# JSBOT_EXTERNAL_READONLY_API_TOKEN=READONLY_STATS_API_TOKEN
# JSBOT_EXTERNAL_READONLY_API_TIMEOUT_MS=10000
# JSBOT_EXTERNAL_READONLY_API_JSON_BASE64=BASE64_ENCODED_EXTERNAL_READONLY_API_CONFIG_JSON
# JSBOT_EXTERNAL_READONLY_API_JSON={"baseUrl":"http://152.53.231.224:8787","token":"READONLY_STATS_API_TOKEN","timeoutMillis":10000}

# Zeabur PostgreSQL service variables. zeabur-template.yaml maps exposed
# PostgreSQL variables into the ZEABUR_POSTGRES_* names below.
# POSTGRES_HOST=${POSTGRES_HOST}
# POSTGRES_PORT=${POSTGRES_PORT}
# POSTGRES_DATABASE=${POSTGRES_DATABASE}
# POSTGRES_USERNAME=${POSTGRES_USERNAME}
# POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
# ZEABUR_POSTGRES_HOST=${POSTGRES_HOST}
# ZEABUR_POSTGRES_PORT=${POSTGRES_PORT}
# ZEABUR_POSTGRES_DATABASE=${POSTGRES_DATABASE}
# ZEABUR_POSTGRES_USERNAME=${POSTGRES_USERNAME}
# ZEABUR_POSTGRES_PASSWORD=${POSTGRES_PASSWORD}

# Optional PostgreSQL pool tuning used when pg.config.json is generated from POSTGRES_* variables.
JSBOT_PG_POOL_MAX=20
JSBOT_PG_IDLE_TIMEOUT_MILLIS=30000
JSBOT_PG_CONNECTION_TIMEOUT_MILLIS=5000
JSBOT_PG_LOGGING=false

NODE_ENV=production
